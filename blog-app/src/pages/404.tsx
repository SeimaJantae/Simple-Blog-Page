import React from "react";

type Props = {};

const NotFound = (props: Props) => {
  return (
    <div className="flex items-center justify-center h-screen">
      <h1 className="text-gray-500 text-lg font-light">404-NotFound</h1>
    </div>
  );
};

export default NotFound;
